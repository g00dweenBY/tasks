/*
char myChar = 'a';
	while (myChar <= 'z')
	{
		cout << myChar << " = " << static_cast<int>(myChar) << endl;
		++myChar;
	}

       int outer = 5;
    while (outer >= 1)
    {
        int inner = 5;
        while (inner >= outer)
            std::cout << inner-- << " ";

        std::cout << "\n";
        --outer;
    }


	int outer = 1;
	while (outer <= 5)
	{
		int inner = 5;
		while (inner >= 1)
		{
			if (inner <= outer)
				cout << inner << " ";
			else
				cout << "  ";
			--inner;
		}

		cout << "\n";
		++outer;
	}

*/