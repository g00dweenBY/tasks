/*

		double light;
	cout << "Введите количество световых лет: ";
	cin >> light;
	astro(light);

	void astro(double lightYear)
{
	double astronomicalUnits = lightYear * 63240;
	cout << lightYear << "световых лет = " << astronomicalUnits << " астрономических единиц" << endl;
}

* void timer(int hours, int minutes)
{
	cout << "Time: " << hours << ":" << minutes << endl;
}
int h, m;
	cout << "Введите часы: ";
	cin >> h;
	if (h >= 24)
	{
		cout << "Вы ввели некорректное количество часов";
		return -1;
	}


	cout << "Введите минуты: ";
	cin >> m;
	if (m >= 60)
	{
		cout << "Вы ввели некорректное количество минут";
		return -1;
	}
	timer(h, m);
*/